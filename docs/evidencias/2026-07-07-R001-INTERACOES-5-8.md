# Evidência — Iterações 5-8: Roles, Auditoria, Frontend

> **Data:** 2026-07-07
> **Requisito:** R001 — Organizações, Logins, Papéis e Permissões
> **Iterações:** 5 (Roles), 6 (Projeto), 7 (Auditoria), 8 (Frontend)
> **Status:** Concluídas (parcial It6)

---

## Iteração 5 — Roles e custom roles

### Entregue
- `GET /api/roles` — lista roles do sistema + custom da organização
- `POST /api/roles` — cria custom role com permissões
- `PATCH /api/roles/{id}` — edita nome, descrição, permissões
- `DELETE /api/roles/{id}` — exclui (bloqueia system roles e roles em uso)
- `GET /api/permissions` — lista todas as permissões disponíveis
- 5 testes unitários (create, empty name, delete system, delete in-use, update system)

## Iteração 7 — Auditoria

### Entregue
- `AuditLog` entidade com OrganizationId, ActorUserId, Action, ResourceType, Metadata
- `IAuditLogWriter` — interface com buffer Write + SaveAsync
- `AuditLogWriter` — implementation que persiste em lote no DbContext
- `IAuditLogReader` — interface com paginação e filtros
- `AuditLogReader` — implementation com query paginada
- `GET /api/audit-logs` — endpoint com filtros (projectId, action, resourceType, page, pageSize)
- 3 testes de entidade

## Iteração 8 — Frontend

### Stack
- React 19 + TypeScript 6 + Vite 8
- Tailwind CSS v4 + @tailwindcss/vite
- React Router DOM v7
- Axios para API calls
- Proxy Vite configurado: `/api` → `http://localhost:5000`

### Telas
| Rota | Componente | Autenticação |
|------|-----------|-------------|
| `/login` | Login.tsx | Pública |
| `/register` | Register.tsx | Pública |
| `/` | Dashboard.tsx | JWT (layout + sidebar) |
| `/members` | Members.tsx | JWT |
| `/roles` | Roles.tsx | JWT |

### Arquitetura
- `lib/api.ts` — axios instance com interceptor JWT + refresh automático
- `lib/auth.tsx` — AuthContext + Provider + useAuth hook
- `lib/types.ts` — interfaces (LoginResponse, Member, Role etc.)
- Rotas protegidas via wrapper que verifica `isAuthenticated`

### Build
```
✓ built in 290ms
dist/index.html                  0.45 kB
dist/assets/index.css           17.40 kB
dist/assets/index.js           295.12 kB
```

## Arquivos criados (backend)

```
src/VoucherSystem.Domain/
  AuditLog.cs          (criado)
  ProjectAccess.cs     (criado)
  UsageQuota.cs        (criado)

src/VoucherSystem.Contracts/
  It5Contracts.cs      (criado — Role, Permission, Audit DTOs)

src/VoucherSystem.Application/
  IRoleService.cs      (criado)
  IRoleRepository.cs   (criado)
  RoleService.cs       (criado)
  IAuditLog.cs         (criado)

src/VoucherSystem.Infrastructure/
  RoleRepository.cs    (criado)
  AuditLogWriter.cs    (criado)
  VoucherSystemDbContext.cs (alterado)
  Migrations/AddIt5It6It7 (criado)

src/VoucherSystem.Api/
  Endpoints/RoleEndpoints.cs    (criado)
  Endpoints/AuditEndpoints.cs   (criado)
  Endpoints/SystemEndpoints.cs  (criado)
  Program.cs                    (alterado)

tests/VoucherSystem.UnitTests/
  RoleServiceTests.cs  (criado — 5 testes)
  It5It6It7Tests.cs    (criado — 3 testes)
```

## Validação final

```
Backend build:    0 errors
Testes:           35 passed
Frontend build:   ✓ (295KB JS + 17KB CSS)
API rodando:      http://localhost:5000
Frontend dev:     http://localhost:5173 (com proxy para API)
```

## Pendências do R001

- [ ] It6 — ProjectAccess endpoints completos + validação por projeto
- [ ] It9 — Hardening (observabilidade, logs estruturados, rate limit, segurança)
- [ ] Convite: reenvio e revogação (endpoints pendentes)
- [ ] E-mails transacionais (requer SMTP)
- [ ] Tela de auditoria no frontend
